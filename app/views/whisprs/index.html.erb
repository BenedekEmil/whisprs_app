<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render 'profile' %>
      <% else %>
        <%= render 'popular' %>
      <% end %>
      <%= render 'feed' %>
      <%= render 'follow_others' %>
    </div>
  </div>
</section>
