<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render 'profile' %>
      <% else %>
        <%= render 'popular' %>
      <% end %>
      <%= render 'feed' %>
      <%= render 'users/show' %>
    </div>
  </div>
</section>
