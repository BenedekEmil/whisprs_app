<section class="section">
  <div class="container">
    <div class="columns">
      <%= render 'profile' %>
      <%= render 'feed' %>
      <%= render 'follow_others' %>
    </div>
  </div>
</section>